diff --git a/ports/glib/0001-unc-helper.patch b/ports/glib/0001-unc-helper.patch
new file mode 100644
index 0000000..ae34aac
--- /dev/null
+++ b/ports/glib/0001-unc-helper.patch
@@ -0,0 +1,115 @@
+diff --git a/glib/glib-init.h b/glib/glib-init.h
+index 2ce2f0d..fb7be0c 100644
+--- a/glib/glib-init.h
++++ b/glib/glib-init.h
+@@ -45,6 +45,7 @@ gboolean _g_win32_call_rtl_version (OSVERSIONINFOEXW *info);
+ 
+ extern HMODULE glib_dll;
+ gchar *g_win32_find_helper_executable_path (const gchar *process_name, void *dll_handle);
++gchar *_g_win32_find_helper_walk_up (gchar *base_searching_path, const gchar *executable_name);
+ int g_win32_reopen_noninherited (int fd, int mode, GError **err);
+ gboolean g_win32_handle_is_socket (void *h);
+ 
+diff --git a/glib/gwin32.c b/glib/gwin32.c
+index c569d9c..a6540e9 100644
+--- a/glib/gwin32.c
++++ b/glib/gwin32.c
+@@ -1446,37 +1446,16 @@ g_crash_handler_win32_deinit (void)
+  * should be deallocated with g_free().
+  */
+ gchar *
+-g_win32_find_helper_executable_path (const gchar *executable_name, void *dll_handle)
++_g_win32_find_helper_walk_up (gchar *base_searching_path,
++                              const gchar *executable_name)
+ {
+   static const gchar *const subdirs[] = { "", "bin", "lib", "glib", "gio" };
+   static const gsize nb_subdirs = G_N_ELEMENTS (subdirs);
+ 
+-  DWORD module_path_len;
+-  wchar_t module_path[MAX_PATH + 2] = { 0 };
+-  gchar *base_searching_path;
+   gchar *p;
+-  gchar *executable_path;
++  gchar *executable_path = NULL;
+   gsize i;
+ 
+-  g_return_val_if_fail (executable_name && *executable_name, NULL);
+-
+-  module_path_len = GetModuleFileNameW (dll_handle, module_path, MAX_PATH + 1);
+-  /* The > MAX_PATH check prevents truncated module path usage */
+-  if (module_path_len == 0 || module_path_len > MAX_PATH)
+-    return NULL;
+-
+-  base_searching_path = g_utf16_to_utf8 (module_path, -1, NULL, NULL, NULL);
+-  if (base_searching_path == NULL)
+-    return NULL;
+-
+-  p = strrchr (base_searching_path, G_DIR_SEPARATOR);
+-  if (p == NULL)
+-    {
+-      g_free (base_searching_path);
+-      return NULL;
+-    }
+-  *p = '\0';
+-
+   for (;;)
+     {
+       /* Search in subdirectories */
+@@ -1507,9 +1486,57 @@ g_win32_find_helper_executable_path (const gchar *executable_name, void *dll_han
+       if (p == NULL)
+         break;
+ 
++      /* Don't strip past the root of a UNC path ("\\\\server\\share").
++       * For drive-letter paths ("C:") the strrchr() == NULL check above
++       * already prevents over-stripping. UNC paths start with a
++       * separator, so without this guard the loop strips past
++       * "\\server\\share" down to "\\server", "\", and finally "" (empty),
++       * which makes g_build_filename() below produce a non-absolute path
++       * and trips the g_assert() above. The separator we're about to strip
++       * is the first one after the server name, i.e. the one preceding the
++       * share name.
++       */
++      if (G_IS_DIR_SEPARATOR (base_searching_path[0]) &&
++          G_IS_DIR_SEPARATOR (base_searching_path[1]) &&
++          strchr (base_searching_path + 2, G_DIR_SEPARATOR) == p)
++        break;
++
+       *p = '\0';
+     }
++
+   g_free (base_searching_path);
++  return executable_path;
++}
++
++gchar *
++g_win32_find_helper_executable_path (const gchar *executable_name, void *dll_handle)
++{
++  DWORD module_path_len;
++  wchar_t module_path[MAX_PATH + 2] = { 0 };
++  gchar *base_searching_path;
++  gchar *p;
++  gchar *executable_path;
++
++  g_return_val_if_fail (executable_name && *executable_name, NULL);
++
++  module_path_len = GetModuleFileNameW (dll_handle, module_path, MAX_PATH + 1);
++  /* The > MAX_PATH check prevents truncated module path usage */
++  if (module_path_len == 0 || module_path_len > MAX_PATH)
++    return NULL;
++
++  base_searching_path = g_utf16_to_utf8 (module_path, -1, NULL, NULL, NULL);
++  if (base_searching_path == NULL)
++    return NULL;
++
++  p = strrchr (base_searching_path, G_DIR_SEPARATOR);
++  if (p == NULL)
++    {
++      g_free (base_searching_path);
++      return NULL;
++    }
++  *p = '\0';
++
++  executable_path = _g_win32_find_helper_walk_up (base_searching_path, executable_name);
+ 
+   if (executable_path == NULL)
+     {
diff --git a/ports/glib/portfile.cmake b/ports/glib/portfile.cmake
index fe8c55e..2f6ff58 100644
--- a/ports/glib/portfile.cmake
+++ b/ports/glib/portfile.cmake
@@ -18,6 +18,7 @@ vcpkg_extract_source_archive(SOURCE_PATH
     PATCHES
         use-libiconv-on-windows.patch
         libintl.patch
+        0001-unc-helper.patch
 )
 
 set(LANGUAGES C CXX)
diff --git a/ports/glib/vcpkg.json b/ports/glib/vcpkg.json
index 2d680e6..61f86df 100644
--- a/ports/glib/vcpkg.json
+++ b/ports/glib/vcpkg.json
@@ -37,5 +37,6 @@
       "description": "Build with selinux support.",
       "supports": "linux"
     }
-  }
+  },
+  "port-version": 1
 }
diff --git a/versions/baseline.json b/versions/baseline.json
index beac9fc..8c9f978 100644
--- a/versions/baseline.json
+++ b/versions/baseline.json
@@ -3450,7 +3450,7 @@
     },
     "glib": {
       "baseline": "2.88.0",
-      "port-version": 0
+      "port-version": 1
     },
     "glib-networking": {
       "baseline": "2.80.1",
diff --git a/versions/g-/glib.json b/versions/g-/glib.json
index 99b712a..76b412f 100644
--- a/versions/g-/glib.json
+++ b/versions/g-/glib.json
@@ -1,5 +1,10 @@
 {
   "versions": [
+    {
+      "git-tree": "42352b4459d7afd23bda5d35927f5ee3bbd5f76b",
+      "version": "2.88.0",
+      "port-version": 1
+    },
     {
       "git-tree": "84c530f543d52372516f5234f2f0ff4a2c2bfe83",
       "version": "2.88.0",
